## Imported Claude Cowork project instructions

---

# 집필 규칙 (2026-08-13 개정)

이 사이트에 콘텐츠를 추가하거나 고치는 모든 작업은 아래 규칙을 따른다.
개정 배경: 2026년 7월 이후 새로 만든 섹션의 약 80%가 터미널 기반 개발 주제(advanced.html)로 쏠렸고,
그 명령어들이 대부분 macOS·Linux 기준이라 윈도우 사용자가 그대로 따라 하면 막히는 상태였다.

## 1. 기준 독자와 환경

**기준 독자** — 윈도우 PC를 쓰는 비개발자 직장인. 터미널을 열어본 적이 없고, 앞으로도 열 생각이 없다.

**기준 환경** — 우선순위대로:

1. 웹 브라우저 (chatgpt.com, claude.ai 등)
2. 데스크톱 앱 / 모바일 앱
3. 윈도우 PC에 설치해서 쓰는 프로그램
4. 터미널 명령 — 다른 방법이 아예 없을 때만

새 섹션을 쓸 때는 1~2번만으로 끝나는 방법이 정말 없는지 먼저 확인한다.
"개발자는 이렇게 합니다"로 시작하는 글은 이 사이트의 기본값이 아니다.

## 2. 터미널·설치가 꼭 필요할 때

주제 자체가 CLI 도구(Claude Code, Codex CLI 등)라면 당연히 터미널을 다뤄야 한다. 그때는:

- **윈도우 기준을 먼저 쓴다.** PowerShell 명령을 본문에, macOS·Linux는 그 옆이나 아래에 병기한다.
  지금까지처럼 bash를 본문에 두고 윈도우를 주석 한 줄로 처리하지 않는다.
- **윈도우에서 안 되는 기능은 그 사실을 먼저 밝힌다.** "이 기능은 윈도우에서 직접 동작하지 않습니다"를
  단계 설명 앞에 두고, WSL2 같은 우회로는 그다음에 안내한다. 다 읽고 나서야 못 쓰는 걸 알게 하지 않는다.
- **경로·단축키·환경변수는 양쪽을 모두 적는다.**
  - 가상환경: `.venv\Scripts\activate` (윈도우) / `source .venv/bin/activate` (맥·리눅스)
  - 환경변수: `setx KEY "값"` (윈도우) / `export KEY="값"` (맥·리눅스)
  - 단축키: `Ctrl+Shift+B` (윈도우) / `Cmd+Shift+B` (맥)
  - 설치: 윈도우는 설치 프로그램(.exe)이나 winget이 있으면 그것을 먼저, `curl ... | bash`는 나중에
- **웹·앱으로 대체할 수 있는 부분은 알려준다.** 예: "MCP 서버를 직접 만들지 않아도, 같은 일을
  Claude 데스크톱 앱의 커넥터 설정에서 클릭으로 할 수 있습니다."

## 3. 신규 콘텐츠 배분

한 주에 새로 만드는 섹션 중 **advanced.html은 3분의 1을 넘기지 않는다.**
나머지는 start / prompting / usecases / claude / chatgpt에 배분한다.
개발 주제가 아무리 새롭고 화제가 되어도, 이 사이트 방문자 대부분에게는 남의 얘기다.

주제 후보를 고를 때 던질 질문: **"윈도우 노트북 한 대와 브라우저만 있는 사람이 오늘 바로 써먹을 수 있나?"**
아니라면 우선순위를 뒤로 민다.

## 4. 기존 섹션 보완

advanced.html의 기존 54개 섹션은 지우지 않는다. 대신 손댈 일이 생길 때마다 다음을 채운다.

- 섹션 첫머리에 **"윈도우에서는"** 한 문단 — 되는지, 안 되는지, 대안은 무엇인지
- 코드 블록에 윈도우 명령 병기
- 웹·앱으로 같은 목적을 달성하는 방법이 있으면 링크

한 번에 다 하려 들지 말고, 점수가 낮거나 방문이 많은 섹션부터 하나씩 처리한다.

## 5. 채점 기준 연동

`scripts/score_content.py`의 루브릭에 **접근 문턱(access)** 항목이 있다(0~2점).
터미널을 요구하면서 윈도우 안내가 없는 섹션은 0점을 받고, 자동으로 개선 우선 목록 위로 올라온다.
새 섹션을 쓴 뒤에는 스크립트를 돌려 이 항목이 2점인지 확인한다.

## 6. 하지 말 것

- 맥·리눅스 명령만 적어두고 "윈도우는 알아서" 넘어가기
- 터미널 설치 과정을 설명 없이 전제하기 (Node.js, Python, Homebrew 등)
- 개발 주제를 연달아 추가해 신규 콘텐츠가 개발 페이지로만 쌓이게 두기
- 웹·앱으로도 되는 일을 굳이 코드로 설명하기
